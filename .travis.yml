addons:
  sonarcloud:
    organization: "braunjhun"
    token:
      secure: SONAR_TOKEN # encrypted value of your token

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - sonar-scanner
language: node_js
node_js:
  - 11